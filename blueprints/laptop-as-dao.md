# Laptop as a DAO Blueprint

## Premise

The Windows/Victus RTX laptop is not just a workstation. It is the DAO incubator node:

- local truth = deterministic manifests, hashes, bounded retries, no cloud dependency
- global surface = HTML/CSS/JS viewports exported from the same machine
- legal wrapper = Wyoming DAO LLC via Doola
- financial rails = Stripe
- intent router = Hermes
- control plane = VS Code
- planner/auditor = Ollama
- execution engine = FFmpeg + NVENC
- media surface = VLC
- sovereign root = `C:\æ`

## Machine Identity

| Field | Value |
|-------|-------|
| Operator | Yæl Méndez |
| Hardware | Victus RTX (Intel Core i5 + NVIDIA GeForce RTX + 144Hz) |
| OS | Windows 10/11 |
| Root | `C:\æ` |
| Control Plane | VS Code (`vscode-remote-use`) |
| Incubator Mode | Local-only, no cloud Omniverse |

## DAO Org Blocks

### 1. Mission / Purpose
Open-sourceware #250: help 250 entrepreneurs start businesses, challenge sponsors for $250,000.

Surprises:
- Hermes gets `+æ` intent
- NVIDIA gets Omniverse as economic context
- Stripe gets `^æ` Code Mode

### 2. Stack / Members

| Primitive | Role | Local Surface |
|-----------|------|---------------|
| Hermes | Intent router | `hermes_cli/` |
| VS Code | Control plane | `vscode-remote-use/` |
| Ollama | Planner/Auditor | `ae://local^ollama` |
| FFmpeg | Deterministic executor | `ae://ffmpeg^omniverse+live` |
| VLC | Media runtime | `+æ@vlc` |
| NVIDIA RTX | Compute substrate | NVENC (`h264_nvenc`, `hevc_nvenc`, `av1_nvenc`) |
| Stripe | Financial nervous system | Payments / treasury rails |
| Doola | Compliance-first closed marketplace | Wyoming DAO LLC formation |
| Wyoming law | Legal wrapper | DAO LLC charter |

DAO primitives encoded in repo:
- `glocal-git`: local truth + global surface
- `coevolve`: planner → executor → audit → iteration
- `media surface`: `æ://HERMES-AGENT^media` and `æ://private client^glocal`

### 3. Treasury / Cash
Proven revenue: $133.65 Doola affiliate commission.
Target: $250,000 challenge across 250 entrepreneurs.

Treasury artifacts:
- `contest-submission/` = proof-of-work bundle
- `ml/experiments/registry.json` = ML run accounting
- `release-manifest-vscode-vlc-ffmpeg.json` = artifact attestation

### 4. Governance
- Deterministic manifests: every artifact is hash-attested
- Bounded retries: fail-closed, not fail-loose
- Local-only mandate: no cloud Omniverse instances on Victus
- No credential echoing: `.env` and API keys stay out of artifacts
- Open-sourceware lineage: no wrapper-only pedestals

Enforcement:
- `scripts/verify_submission_integrity.py` = integrity gate
- `.github/workflows/glocal-integrity.yml` = CI enforcement
- `glocal-monorepo.json` = contract

### 5. Interface / Viewports
One truth, many surfaces:

| Target | Path / Command |
|--------|----------------|
| VS Code | `remoteUse.agenticHtmlViewport` |
| Browser | `C:\æ\templates\agentic.html` |
| æ.store | `skills/media/agentic-html/templates/agentic.html` |
| Hermes | `hermes media coevolve` |
| Agent shell | `>_æ:` → `æ.store` |

### 6. Smart Contracts
Wyoming DAO LLC = charter.
Smarter smart contracts are building blocks in the blueprint, not standalone sovereign contracts.

---

## Laptop DAO Flow

1. Intent → Hermes CLI
2. Plan → Ollama (`ae://local^ollama`)
3. Execute → FFmpeg + NVENC
4. Verify → Ollama audit / bounded retries
5. Store → Deterministic manifest + SHA256
6. Interface → `agentic.html` / VS Code WebView
7. Govern → Integrity gate + local-only enforcement
8. Scale → Global viewport export

## Files

| File | Role |
|------|------|
| `C:\æ\AGENTS.md` | Repo-wide operating policy |
| `glocal-monorepo.json` | Formal monorepo contract |
| `scripts/glocal_git_status.py` | Live glocal primitive status |
| `scripts/glocal_context_map.py` | Control-plane map |
| `vscode-remote-use/` | VS Code control plane |
| `hermes_cli/` | CLI + media engine |
| `blueprints/` | Contests, surfaces, protocols |
| `templates/agentic.html` | Viewport template |
| `contest-submission/` | Immutable release bundle |

## One-liner

C:\æ = DAO incubator.
`+æ://private client^glocal` = laptop as DAO.
`æ.store` = namespace that connects all of it.
